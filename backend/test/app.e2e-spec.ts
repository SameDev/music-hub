// Placeholder for future e2e tests — real test infrastructure (test DB, fixtures)
// lands alongside the first feature module (Auth).
describe('app bootstrap placeholder', () => {
  it.todo('add e2e tests once the Auth module lands');
});
